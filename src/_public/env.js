// env.js
window.__env__ = Object.freeze({
  // biome-ignore lint/style/useNamingConvention: suppress capitalized key
  EXPLANATION: 'All variables are injected in pipeline during deployment time',
});
